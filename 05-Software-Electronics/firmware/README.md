# Firmware

Microcontroller code (Arduino / ESP32 / Raspberry Pi / etc.) for the room's
props and effects. Individual mini-programs may also live with their puzzle
under [`../../04-Puzzles/`](../../04-Puzzles/) — link between them either way.

Linked from [`../README.md`](../README.md). Pairs with [`../hardware/`](../hardware/)
(the wiring) and [`../show-control.md`](../show-control.md) (the triggering logic).

## To capture here

- One subfolder per prop's firmware sketch/program
- Build/flash notes and pin mappings (kept in sync with [`../hardware/`](../hardware/))
- Shared libraries/helpers reused across props

## Status

Placeholder — no firmware written yet.
